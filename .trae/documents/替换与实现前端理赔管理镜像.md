## 目标
- 完全按 `/Users/zhaomosheng/Desktop/LiqPass-clean/.trae/documents/2e.md` 的 UI/交互重写理赔管理页。
- 先不接后端；实现“下单后订单同时出现在订单管理与赔付管理”。

## 具体改动
1. 用文档中的 `ClaimsManagePage` 代码覆盖 `apps/us-frontend/src/pages/ClaimsManage.tsx`，默认导出保持一致。
2. 路由保持不变：`/claims` 指向新的页面（`apps/us-frontend/src/App.tsx` 不改）。
3. 数据源小调整以满足“同步出现”：
   - 将文档里的 `mockOrders` 替换为从本地存储读取：`const orders = JSON.parse(localStorage.getItem('lp_local_orders')||'[]')`，并用 `useEffect` 订阅或轮询以在下单后刷新。
   - 数据字段按文档结构映射（`productName/principalUsd/.../orderRef/remainingSeconds/status`）。缺失字段用占位或默认值。
4. 验证与标记赔付按钮行为保持文档逻辑，先只更新前端状态。

## 兼容性与最小改动
- 不创建新文件；不更改其他页面和购买流程；保证“只修改这个”。
- 如现有 `ClaimsPage.tsx`（`/claims/new`）仍保留，不影响本次实现。

## 验证方式
- 执行一次用户购买（沿用现有下单流程），确认：
  - 订单在 `/orders` 正常显示。
  - 打开 `/claims`，同一订单以文档示例样式显示。

## 后续扩展（待你确认后再做）
- 接入 `/api/v1/claims/verify`、`/api/v1/claims/settle` 等后端接口替换前端占位逻辑。
- 与 `ClaimsPage.tsx` 合并入口或保持双入口。