## 目标
- 前端“赔付管理/订单管理/API 设置/退出”已集中在钱包菜单下（`Header.tsx:55–69, 71–86`），仅需路径直达小调整即可满足导航预期。
- 重点交付：打通用户理赔与管理员人工验证、放款的完整链路，前后端接口一致、状态闭环可追溯。

## 前端改造
- 菜单入口直达：将 `ACCOUNT_MENU_ITEMS` 的 `to` 改为 `/orders`、`/claims`、`/settings/api`（`constants.ts:38–42`），去除 `/account/...` 重定向依赖。
- 用户理赔页对接：
  - `ClaimsPage.tsx` 使用 `authFetch` 调用 `POST /api/v1/claims`（创建）、`POST /api/v1/claims/prepare`、`POST /api/v1/claims/verify`（校验），根据返回渲染验证结果与额度（`App.tsx:46, 51–53`）。
  - 成功付款后保留链上交易信息并写入本地，页面跳转 `/claims` 并给出 Toast 提示（`ToastContext.tsx:34–45`）。
- 管理理赔页对接：
  - `ClaimsManage.tsx` 用 `authFetch` 对接 `GET /api/v1/admin/claims`（分页）、`POST /api/v1/admin/claims/:claimId/verify`（人工验证）、`POST /api/v1/admin/claims/:claimId/mark-paid`（标记放款）。
  - 移除本地模拟 `handleVerify/handleMarkPaid`，改为真实接口调用并刷新列表，保持 `sticky top-0 z-40` 头部不遮挡菜单。
- 细节一致性：
  - Toast 与下拉层级：菜单 `z-[9999]`，Toast 容器 `z-[100]`（不拦截点击），确保右上角正常点击。
  - Switch 交互如需“圆圈可点”，考虑去除 `Thumb` 的 `pointer-events-none`（`components/ui/Switch.tsx:18–22`），或在轨道上统一处理点击事件。

## 后端对接
- 用户接口（JWT）：
  - `POST /api/v1/claims`、`GET /api/v1/claims`、`GET /api/v1/claims/:claimId`、`POST /api/v1/claims/:claimId/submit`、`POST /api/v1/claims/prepare`、`POST /api/v1/claims/verify`（`routes/claims.ts`）。
- 管理接口（JWT+API Key）：
  - `GET /api/v1/admin/claims`、`POST /api/v1/admin/claims/:claimId/verify`、`POST /api/v1/admin/claims/:claimId/mark-paid`（`routes/adminClaims.ts`）。
- 鉴权中间件：`enhancedAuth.ts` 的 `claimAuth/adminAuth` 与 `requireAdminApiKey` 正确组合。

## 验证与回归
- 用真实数据跑通用户侧“创建→验证→查看详情”，管理员侧“审核→放款→状态更新”。
- 交叉验证路由跳转（`App.tsx:39–61`）与菜单点击（`Header.tsx:55–69, 71–86`），确保不被页面 `z-40` 顶部条遮挡。
- 验证 Toast 出现时右上角菜单仍可交互；必要时下调 Toast 层级或调整位置。

## 交付与记录
- 以最小提交粒度完成前端对接与路径调整，提交信息包含“赔付管理接口对接与菜单路径直达”。
- 记录接口与状态机说明，补充到现有开发文档位置（不新增冗余文档）。