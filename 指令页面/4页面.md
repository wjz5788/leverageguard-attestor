收到。直接开干，今天就能推进的清单在这——拷到你的仓库当 Issues/PR 描述就能用了。

## 今日必做（D1）

**PR#1 · 启动与下单强校验（VAULT/CHAIN/USDC）**

* [ ] 后端启动时：若 `PAYMENT_VAULT_ADDRESS / PAYMENT_CHAIN_ID / USDC_ADDRESS` 缺失或非法 → 进程拒启并给出明确错误。
* [ ] 下单接口：把 `to/token/chainId/amount/nonce/expiry` 写入 `orders.quote_*`，用于后续比对。
* **验收**：错误配置 → 启动失败；伪造下单参数 → 422 并记录 `audit_events`.

**PR#2 · 鉴权与防重放（Orders/Claims/Payouts 全线）**

* [ ] 所有写操作路由要求 `Authorization: Bearer <JWT>`。
* [ ] 增加 `Client-Nonce` 校验（每用户去重，TTL=1h）。
* **验收**：匿名 401；过期/无效 JWT 401；重复 nonce 409。

**PR#3 · 支付回执严校（链上对齐报价）**

* [ ] 通过 txHash 读取链上回执，逐字段核对 `to/token/amount/chainId` 与 `orders.quote_*`。
* [ ] 状态机：`created → paid_verified → …`；错配直接拒绝并打点。
* **验收**：三组错配样例（错链/错 token/错 to）全部拦截。

## 明日交付（D2）

**PR#4 · 证据序列化/哈希规范 v1**

* [ ] Canonical JSON：键名 ASCII 排序、数值转字符串、UTC 毫秒时间戳、空值省略。
* [ ] 产出 ≥3 组测试向量（普通平仓/强平或 ADL/部分成交），TS 与 Python 计算 keccak 一致。
* **验收**：同一证据多次序列化哈希不变；跨语言一致。

## 本周收口（D4–D7）

**PR#5 · CORS/限流/（必要处）CSRF**

* [ ] Origin 白名单；滑动窗口限流（用户/IP）；高频路由返回 `429 + Retry-After`。
* **验收**：跨域伪造被拒；10× 峰值压测不击穿。

**PR#6 · 幂等键（Idempotency-Key）**

* [ ] 创建订单/赔付等写操作接入幂等键；建立 `idempotency_keys` 表；命中返回首次结果。
* **验收**：并发重放 0 次重复资源，响应头含 `Idempotent-Replay: true`。

**PR#7 · 日志 JSON 化与脱敏**

* [ ] 统一结构化日志；API Key/个人信息脱敏；禁止明文秘钥落盘。
* **验收**：抽样日志不含敏感明文。

**PR#8 · 赔付与对账闭环**

* [ ] 建立 `payout_txs / audit_events`；透明度页改读后端聚合视图（去 Mock）。
* **验收**：对账视图“期初+入账−出账±误差=期末”成立；误差阈值超限触发告警。

---

### 你现在可以直接做的两步

1. 在仓库里新建 8 个 Issue，标题就用上面 PR 名称，勾选清单即验收口径。
2. 把 D1 的三条拆成三个独立 PR（小步快跑），合并后立刻跑一次“下单→支付校验”的回归流。

有阻力点（比如链上回执取数、JWT 颁发、幂等键落库）告诉我，我直接给你对应的最小实现轮廓与测试用例描述，保证不跑题、当天可合并。
