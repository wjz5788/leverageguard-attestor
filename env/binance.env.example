# Binance Futures API credentials for liquidation verification scripts
# Copy to .env or export as environment variables before running scripts.
BINANCE_API_KEY="your_read_only_api_key"
BINANCE_SECRET_KEY="your_read_only_api_secret"
# Optional: location of the credentials file (overrides default search order)
# BINANCE_ENV_FILE="/secure/path/binance.env"
